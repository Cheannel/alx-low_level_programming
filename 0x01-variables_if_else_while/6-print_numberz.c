#include <stdio.h>
/**
 * main - entry point
 * Description: printing all single digit numbers
 * Return: (0)
 */
int main(void)
{
	int x;

	for (x = '0'; x < 10; x++)
	{
		putchar(x);
	}
	putchar(x);
	return (0);
}

