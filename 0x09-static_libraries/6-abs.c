#include "main.h"
/**
 * _abs - entry point
 * Description: computing the absolute value of an integer
 * @n: integer value to be computed
 * Return: 1 or -1
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
